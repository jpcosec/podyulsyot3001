% Leave empty
\name{}{}
\title{}
\photo[]{}


% Reduce the length of the footer
\setlength{\footskip}{-45pt}

%Define the classic red modern cv theme
\moderncvtheme[red]{classic}

%Definition of a new highlight color instead of the previously defined one
\definecolor{MyColor}{RGB}{0, 0, 0} % MyColor [Name of the variable]; R = 0, G = 0, B = 0
% Apply the new created color "MyColor" as the default highlight color
\colorlet{color1}{MyColor}

%Set the left column to the width of the longest used word, in this case "Staatsangehörigkeit"
\sethintscolumntowidth{Staatsangehörigkeit}

% Define bullet point icon
\renewcommand{\labelitemi}{-}

% Definition of new commands to create the CV title and signature
\newcommand{\myPhoto}[1]{\includegraphics[width = 0.2\textwidth]{#1}}

\newcommand{\myName}[2]{\adjustbox{max width=\linewidth}{\mbox{{\sffamily\fontsize{30}{36}\selectfont #1 #2}}}}

\newcommand{\myCVtitle}[4][Lebenslauf]{
 \begin{minipage}[b]{0.6\textwidth}
 \myName{#2}{#3}\\[10pt]
    {\color{MyColor}\huge \textit{#1}}
 \end{minipage}
 \hfill
 \fcolorbox{MyColor}{white}{\myPhoto{#4}}

 \vspace{2em}
 }

% ATS-friendly contact item: label and value on the same text line
\newcommand{\cvcontact}[2]{\noindent\textbf{#1:} #2\par\vspace{3pt}}

% ATS hidden text: white, same size as context — invisible on white paper, merges
% into surrounding text run so pdftotext extracts it on the same line
\newcommand{\atswhite}[1]{{\color{white}#1}}

\newcommand{\sign}[3]{
    \vfill
    \begin{FlushRight}
    #2, \today, #1
    \end{FlushRight}
    \vfill
}
