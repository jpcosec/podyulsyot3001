% Definition of the class moderncv with some parameters. Font size 11pt, paper size A4, font sans
\documentclass[11pt, a4paper, sans]{moderncv}

$if(latex_assets_dir)$
% Includes all the necessary packages from the bundled legacy assets
\input{$latex_assets_dir$/Einstellungen/packages}

% Defines commands and parameters for modifying the included packages
\input{$latex_assets_dir$/Einstellungen/commands}
$else$
\input{Einstellungen/packages}
\input{Einstellungen/commands}
$endif$

% Adjust hint column to the longest expected label
\sethintscolumntowidth{$if(hint_width)$$hint_width$$else$Work Authorization$endif$}

% ATS-safe helpers for semantic Markdown blocks mapped by Lua
\newcommand{\cvjob}[5]{%
    \cventry{ #1 }{ #2 }{ #3 }{ #4 }{}{ #5 }
}

\newcommand{\cvedu}[6]{%
    \cventry{ #1 }{ #2\ifx&#3&\else\ (#3)\fi }{ #4 }{ #5 }{}{ #6 }
}

\newcommand{\myCVtitleSingle}[3][Curriculum Vitae]{
 \begin{minipage}[b]{0.6\textwidth}
 \myName{#2}{}\\[10pt]
    {\color{MyColor}\huge \textit{#1}}
 $if(tagline)$\\[8pt]{\large $tagline$}$endif$
 \end{minipage}
 $if(photo)$
 \hfill
 \fcolorbox{MyColor}{white}{\myPhoto{#3}}
 $endif$

 \vspace{2em}
}

$if(resource_dir)$
\graphicspath{{$resource_dir$}}
$endif$
$if(latex_assets_dir)$
\graphicspath{{$latex_assets_dir$/Abbildungen/}}
$endif$

% Beginning of document
\begin{document}
\pagestyle{empty}

% Creates the CV title with name and photo
\myCVtitleSingle[$if(title)$$title$$else$Curriculum Vitae$endif$]{ $if(preferred_name)$$preferred_name$$else$$if(author)$$for(author)$$author$$sep$ $endfor$$endif$$endif$ }{$if(photo)$$if(resource_dir)$$resource_dir$$endif$$photo$$else$Abbildungen/profile.jpg$endif$}

% Reduce the vertical space by 10 points
\vspace*{-10pt}

% Personal information section
\section{$if(contact_section_title)$$contact_section_title$$else$Contact$endif$}
$if(address)$\cvcontact{$if(address_label)$$address_label$$else$Address$endif$}{$address$}$endif$
$if(phone)$\cvcontact{$if(phone_label)$$phone_label$$else$Phone$endif$}{$phone$}$endif$
$if(email)$\cvcontact{$if(email_label)$$email_label$$else$Email$endif$}{$email$}$endif$

$body$

\end{document}
