# Conceptual Motors

## Explanation

The architecture mentions future `browseros/agent`, `os_native_tools`, and `vision` motors, but no stubs or packages exist yet. That leaves the roadmap unrepresented in code.

## Reference in src

- `src/automation/motors/`

## What to fix

Add minimal package stubs and protocol-aligned placeholders for the planned motors.

## How to do it

Create empty-but-documented motor packages with provider/session skeletons, module READMEs, and tests asserting they are intentionally unimplemented rather than accidentally missing.

## Does it depend on another issue?

No.
