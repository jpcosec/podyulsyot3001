# UI Planning Docs

This folder contains UI/workbench planning, redesign, migration, and future implementation material.

Current sandbox behavior belongs in `docs/ui/` and near the code under `apps/review-workbench/`.
