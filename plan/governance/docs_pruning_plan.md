# Documentation Pruning Plan

Planning/governance notes for documentation refactors.

Moved from `docs/index/pruning_plan.md` to keep `docs/` current-state only.

## Current policy

1. `docs/` documents implemented behavior.
2. `plan/` documents migration plans and governance actions.

## Next governance actions

1. Add protocol checks that reject plan-like documents in `docs/`.
2. Keep canonical map/index references aligned with the docs/plan boundary.
